
  # Assign Interactions by Frame Names

  This is a code bundle for Assign Interactions by Frame Names. The original project is available at https://www.figma.com/design/iRKNOsGn1XHn1GPO7579tP/Assign-Interactions-by-Frame-Names.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  